import styled from 'styled-components';

const Line = styled.div`
  width: 100% - 38px;
  height: 0.1vh;
  background-color: #8382ff;
  margin: 20px; // 가운데 정렬을 위해 margin을 auto로 설정하고, 상하 패딩을 20px로 설정
  
`;

export default Line;
