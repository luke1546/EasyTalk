<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.easyback.study.model.mapper.StudyMapper">
  <select id="getList" resultType="String">
    select user_name
    from test
  </select>

  <select id="getWordList" parameterType="wordDto" resultType="wordDto">
    select *
    from words
    where level = #{level}
  </select>

  <insert id="insertWord" parameterType="wordDto">
    insert into words
        (word, pronunciation, level, wordAudioUri)
    values
        (#{word}, #{pronunciation}, #{level}, #{wordAudioUri})
  </insert>

  <insert id="insertMeaning" parameterType="wordMeaningDto">
    insert into words_meaning(wordId, meaning, partOfSpeech)
    values(
            (
              select wordId
              from words
              where word = #{word}
            ),
            #{meaning},
            #{partOfSpeech}
          )
  </insert>
  <update id="updateUri" parameterType="String">
    update words
    set wordAudioUri=
          concat("/study/word/audio/",
                 (select wordId
                  from words
                  where word = #{word}),".wav")
    where word = #{word}
  </update>
    <select id="checkWord" resultType="int" parameterType="String">
        select count(*)
        from words
        where word=#{word}
    </select>
</mapper>
